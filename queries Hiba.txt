
-- create
CREATE TABLE EMPLOYEE (
  empId INTEGER PRIMARY KEY,
  name TEXT NOT NULL,
  dept TEXT NOT NULL,
  salary INTEGER ,
  YOE INTEGER 
);

-- insert
INSERT INTO EMPLOYEE VALUES (0001, 'Clark', 'Sales',10000,1);
INSERT INTO EMPLOYEE VALUES (0002, 'Dave', 'Accounting',15000,2);
INSERT INTO EMPLOYEE VALUES (0003, 'Ava', 'Sales',30000,5);
INSERT INTO EMPLOYEE VALUES (0004, 'Tom', 'clerk',10000,1);

-- fetch 
SELECT * FROM EMPLOYEE WHERE dept = 'Sales' AND salary > 15000 ;
SELECT * FROM EMPLOYEE ORDER BY name;
SELECT * FROM EMPLOYEE ORDER BY salary desc;
-- UPDATE
UPDATE EMPLOYEE 
SET salary=35000
WHERE empID=0004;

SELECT * FROM EMPLOYEE
LIMIT 3;

SELECT MAX(salary) AS MaxSalary FROM EMPLOYEE; 
SELECT Min(salary) AS MaxSalary FROM EMPLOYEE; 
SELECT COUNT(empID) FROM EMPLOYEE;
SELECT AVG(salary) FROM EMPLOYEE;
SELECT SUM(salary) FROM EMPLOYEE;
SELECT * FROM EMPLOYEE
WHERE name LIKE 'a%';
SELECT * FROM EMPLOYEE
WHERE name LIKE '%e';
SELECT * FROM EMPLOYEE
WHERE dept IN ('sales');
SELECT * FROM EMPLOYEE
WHERE YOE BETWEEN 3 AND 5;

CREATE TABLE Persons (
    PersonID int,
    LastName varchar(255),
    FirstName varchar(255),
    state varchar(255),
    City varchar(255)
);

INSERT INTO Persons VALUES (0001,'JOSEPH','ANNA','KERALA','kochi');
INSERT INTO Persons VALUES (0002,'M','SANDRA','KERALA','KANNUR');
INSERT INTO Persons VALUES (0001,'KUMAR','RAJ','KARNATAKA','BELLANDUR');

SELECT * FROM Persons;
SELECT EMPLOYEE.empID,Persons.FirstName
FROM EMPLOYEE
INNER JOIN Persons ON EMPLOYEE.empID = Persons.PersonID;
SELECT EMPLOYEE.empID,Persons.FirstName
FROM EMPLOYEE
LEFT JOIN Persons ON EMPLOYEE.empID = Persons.PersonID;
SELECT EMPLOYEE.empID,Persons.FirstName
FROM EMPLOYEE
CROSS JOIN Persons ;
SELECT name FROM EMPLOYEE
UNION
SELECT FirstName FROM Persons;
SELECT COUNT(empID)
FROM EMPLOYEE
HAVING COUNT(empID) > 5
ORDER BY COUNT(empID) DESC;
